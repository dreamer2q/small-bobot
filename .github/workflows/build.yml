name: Build & Deploy

on:
  push:
    branches: [ master ]

jobs:
  build:
    name: Build & Deploy
    runs-on: ubuntu-latest
    steps:

      - name: Check out code into the Go module directory
        uses: actions/checkout@v2

      - name: Set up Go Env
        uses: actions/setup-go@v2.1.3
        id: go
        with:
          go-version: "1.15.5

      - name: Get dependencies
        run: |
          go get -v -t -d ./...
          if [ -f Gopkg.toml ]; then
              curl https://raw.githubusercontent.com/golang/dep/master/install.sh | sh
              dep ensure
          fi

      - name: Build
        run: go build -tags netgo -v -o main .

      - name: Publish Docker
        uses: elgohr/Publish-Docker-Github-Action@master
        with:
          name: registry.redhome.cc/redhome/hduin-qq-robot
          registry: registry.redhome.cc
          username: ${{ secrets.DOCKER_USER }} # 用户名
          password: ${{ secrets.DOCKER_PASS }} # 密码
          dockerfile: Dockerfile
          snapshot: true

      - name: Deploy
        run: |
          # sleep 1
          # curl -d "" https://port.redhome.cc/api/webhooks/d2dd60c2-4c4e-4d02-9b10-0506349c1cf0
